# OilLens 기술 구현 상세 (코드베이스 기반 추출)

## 1. 프로젝트 개요
- **서비스명**: OilLens (글로벌 원유 가격 인텔리전스 플랫폼)
- **팀명**: 페트로피디아
- **버전**: 0.3.0
- **기술 스택**: React + TypeScript (Vite) / FastAPI (Python) / PostgreSQL 16 (Docker) / Ollama (Gemma 4) / Google Gemini API (Fallback)

## 2. 백엔드 서비스 목록 (backend/app/services/)

| 서비스 | 파일 | 설명 |
|--------|------|------|
| DataCollector | data_collector.py (24KB) | 외부 API 데이터 수집기 (NYT, Guardian, EIA, FRED, Opinet) |
| OpinetCollector | opinet_collector.py (5KB) | 한국석유공사 오피넷 유가 스크래핑 수집기 |
| NewsClassifier | news_classifier.py (27KB) | Gemma 4 뉴스 요인 분류기 (Ollama 로컬 LLM) |
| ForecastEngine | forecast_engine.py (38KB) | XGBoost 기반 AI Quant 예측 엔진 (Method A) |
| FundamentalForecast | fundamental_forecast.py (26KB) | 수급 기반 펀더멘탈 예측 엔진 (Method B) |
| FeatureEngineering | feature_engineering.py (5KB) | 시계열 피처 엔지니어링 파이프라인 (유종별 독립 피처 ~56개 생성) |
| MarketMemory | market_memory.py (11KB) | ChromaDB 벡터 검색 (유사 사례 탐색) |
| BriefingGenerator | briefing_generator.py (18KB) | AI 일일 브리핑 생성기 (Gemma 4) |
| Scheduler | scheduler.py (17KB) | APScheduler 데이터 수집 자동화 스케줄러 |
| GapRecovery | gap_recovery.py (18KB) | 데이터 누락 진단 및 자동 복구 서비스 |
| RateLimiter | rate_limiter.py (8KB) | API별 Rate Limit 중앙 관리 + Exponential Backoff |
| ArchiveCrawler | archive_crawler.py (5KB) | 과거 뉴스 아카이브 크롤러 (OilPrice.com) |
| HistoricalLoader | historical_loader.py (13KB) | ChromaDB 시드 데이터 로더 (주요 유가 사건 30건) |
| ExchangeRateCollector | exchange_rate_collector.py (6KB) | 한국수출입은행 환율 API 수집기 (data.go.kr 공공데이터) |
| ImportConcentration | import_concentration.py (5KB) | 수입 집중도(HHI) 분석 서비스 (KNOC 공공데이터) |

## 3. REST API 엔드포인트 (backend/app/api/)

| 라우트 파일 | 주요 엔드포인트 |
|-------------|----------------|
| prices.py (4KB) | GET /api/prices/latest — 최신 유가, GET /api/prices/history — 기간별 유가 |
| news.py (15KB) | GET /api/news — 뉴스 검색/필터링, 요인별 분류 조회 |
| forecast.py (12KB) | GET /api/forecast — 예측 결과 (Method A + B + News Adjustment) |
| briefing.py (3KB) | GET /api/briefing — AI 일일 브리핑 조회, POST /api/briefing/generate — 수동 생성 |
| public_data.py (4KB) | GET /api/public-data/exchange-rate — KRW/USD 환율, GET /api/public-data/import-concentration — 수입집중도(HHI), GET /api/public-data/data-sources — 공공데이터 목록 |
| admin.py (53KB) | 관리자 API (모니터링, 파이프라인, 크롤링, Gap Recovery) |

> **Note**: `GET /api/health` 헬스체크 엔드포인트는 `main.py`에 직접 등록되어 있으며, 버전, 스케줄러 상태, DB 연결 상태, 필수 환경변수 누락 여부를 반환합니다.

## 4. 데이터베이스 모델 (backend/app/models/)

| 모델 | 테이블명 | 설명 |
|------|---------|------|
| OilPrice | oil_prices | 일별 유종별 현물가 (Dubai, Brent, WTI) |
| NewsArticle | news_articles | 뉴스 기사 + AI 분류 결과 (classification_result JSONB) |
| OilInventory | oil_inventories | 미국 원유 재고 주간 데이터 (EIA) |
| OilProduction | oil_productions | 미국 원유 생산량 주간 데이터 (EIA) |
| MacroIndicator | macro_indicators | 거시경제 지표 (금리, 달러 인덱스, **KRW/USD 환율**) |
| OilImport | oil_imports | 한국석유공사 원유수입 국가별 통계 (1980~2024, 48개국) |
| WorldOilTrade | world_oil_trades | 한국석유공사 세계 원유 수출입 물량 (21개 수출국 × 15개 수입국) |
| PipelineJob | pipeline_jobs | 파이프라인 작업 이력 |
| CollectionLog | collection_logs | 수집 이력 로그 |
| RateLimitCounter | rate_limit_counters | API별 일일 호출 카운터 |

## 5. 프론트엔드 컴포넌트 (frontend/src/components/)

| 컴포넌트 | 설명 |
|---------|------|
| Dashboard | 메인 대시보드 레이아웃 + 예측 비교 테이블 (ForecastComparison) + **USD/KRW 통화 토글** |
| PriceChart | 캔들스틱 유가 차트 (lightweight-charts) + 신뢰구간 밴드 |
| NewsExplorer | 뉴스 익스플로러 & 카테고리 필터 + 유사 사례 팝업 |
| FactorGauge | 6대 요인 게이지/레이더 차트 |
| Briefing | AI 브리핑 뷰어 (유종별 독립 전망 리포트) |
| Admin | 관리자 대시보드 (Command Center, Pipeline, Data Hub, Operations) |

## 6. 핵심 AI/ML 파이프라인

### 6-1. 뉴스 분류 파이프라인
1. NYT + Guardian에서 원유 관련 기사 수집
2. Gemma 4 (Ollama 로컬)로 6대 요인 분류: geopolitics, supply, demand, macro, climate, speculation
3. 유종별(Dubai/Brent/WTI) 독립 영향도 평가 (Impact Score -5~+5, 방향성, 근거)
4. 분류 결과를 PostgreSQL JSONB 컬럼에 저장
5. ChromaDB에 벡터 임베딩 적재

### 6-2. 가격 예측 파이프라인 (듀얼 방법론)
- **Method A (AI Quant)**: XGBoost 6개 독립 모델 (3유종 × 7일/30일)
  - 피처: 이동평균(5/10/20/50일), 변동률(1/5/10/20일), 변동성(10/20일), 유종 간 스프레드, 거시경제 지표, **KRW/USD 환율 및 환율 5일 변화율**
- **Method B (펀더멘탈)**: 5대 시그널 가중 합산
  - 재고 변화, 생산량 추세, 계절 패턴, 평균 회귀(Ornstein-Uhlenbeck), 달러 역상관
  - 달러 시그널에 원/달러 환율 보조 정보 주입 (한국수출입은행 공공데이터)
- **News Adjustment**: ChromaDB 유사 사례 검색 → 유종별 News Premium/Discount (%) 산출
- **최종 산출**: Final Price = Baseline × (1 + News Adjustment%), Confidence Band = ± (RMSE + 불확실성)

### 6-3. RAG 기반 Market Memory
- ChromaDB에 뉴스 임베딩 + 당시 유가 변동률을 매핑하여 적재
- 주요 유가 사건 30건 시드 데이터 사전 적재
- 유사 과거 사례 cosine similarity 검색 (유종별 필터)
- 예: "2019 사우디 아람코 피격 +15% → 현재 유사 패턴 감지"

## 7. 활용 데이터 소스 상세

### 공공 데이터
| 소스 | 제공 기관 | 데이터 | 접근 방식 | Rate Limit |
|------|----------|--------|-----------|-----------|
| Opinet | 한국석유공사 | Dubai/Brent/WTI 일별 현물가 (USD) | 웹 스크래핑 (BeautifulSoup) | 제한 없음 |
| EIA API v2 | 미국 에너지정보청 | 원유 재고(WCESTUS1), 생산량(WCRFPUS2) | REST API (무료 키) | 무제한 (1.5초 간격) |
| FRED API | 미국 연방준비은행 | 연방기금금리(FEDFUNDS), 달러인덱스(DTWEXBGS) | REST API (무료 키) | 무제한 (2.0초 간격) |
| **한국수출입은행** | **공공데이터포털 (data.go.kr)** | **KRW/USD 매매기준율 (일별)** | **REST API (공공데이터 키)** | **1,000회/일** |
| **한국석유공사 (KNOC)** | **공공데이터포털 (data.go.kr)** | **원유수입 국가별 통계, 세계 원유 수출입 물량** | **파일 데이터 (CSV)** | **파일 다운로드** |

### 민간/해외 공공 데이터
| 소스 | 데이터 | Rate Limit |
|------|--------|-----------|
| New York Times Article Search API | 원유 관련 영문 뉴스 기사 | 500회/일 |
| The Guardian Open Platform API | 원유 관련 영문 뉴스 기사 | 5,000회/일 |

## 8. 인프라 구성
- **데이터베이스**: PostgreSQL 16 (Docker 컨테이너, asyncpg 비동기 드라이버)
- **벡터 DB**: ChromaDB (로컬 임베딩, 서버리스 모드)
- **LLM 서버**: Ollama Native App (Apple Silicon Metal 가속)
- **스케줄러**: APScheduler (AsyncIOScheduler, 기본 6시간 주기)
- **프론트엔드**: Vite Dev Server (port 5173)
- **백엔드**: Uvicorn (port 8000)

## 9. 회복탄력성 (Resilience) 설계
- Rate Limit 중앙 관리 (rate_limiter.py) — API별 일일 한도 + Exponential Backoff
- 외부 API 장애 시 로컬 캐시 Fallback (data/raw/ 디렉터리)
- **환율 API 주말/공휴일 빈 응답 시 JSON 캐시 Fallback** (직전 영업일 환율 사용)
- LLM 서킷 브레이커 — 3회 실패 후 AI 모듈 비활성화 (Graceful Degradation)
- Pydantic 스키마 검증 — 모든 입출력 데이터 Validation
- React ErrorBoundary — 컴포넌트별 부분 장애 격리

## 10. 공공데이터 통합 파이프라인 (v0.3.0 신규)

### 한국수출입은행 환율 API
- 일별 KRW/USD 매매기준율 자동 수집 (24시간 주기, APScheduler)
- JSON 캐시 (`data/raw/{date}_krw_usd.json`) + PostgreSQL (`macro_indicators.krw_usd`)
- 주말/공휴일 빈 응답 시 직전 영업일 캐시 자동 사용 (Fallback)
- Rate Limit: 1,000회/일, 1초 간격 제어

### 한국석유공사(KNOC) CSV 적재
- `scripts/load_public_data.py` — EUC-KR 인코딩 CSV 자동 감지 → PostgreSQL upsert
- 원유수입 국가별: 1980~2024년, 48개국, 1,109건
- 세계 수출입 물량: 21개 수출국 × 15개 수입국, 138건

### 수입 집중도(HHI) 분석
- Herfindahl-Hirschman Index: 국가별 수입량 점유율² 합산
- 위험도 판정: high(≥2500) / medium(1500~2499) / low(≤1499)
- 지정학적 리스크의 정량 지표로 활용

### USD/KRW 통화 토글 (Frontend)
- ForecastComparison 카드에 USD/KRW 전환 버튼 배치
- 한국수출입은행 환율 기반 실시간 원화 환산 (₩ 접두사, 천단위 콤마)
- 환율 배지(`1 USD = ₩X,XXX 한국수출입은행`) 표시
- 환율 로드 실패 시 토글 자체를 숨김 (Graceful Degradation)
